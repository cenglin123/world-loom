---
model: claude-opus
generated_at: 2026-08-03T00:00:00Z
type: plan
title: 方法论下沉批次二
---

# 方法论下沉批次二

> **解锁条件**：方法卡库经一轮真实写作使用后。

批次一（[[docs/plans/completed/20260803-方法卡库]]）验证了按需检索能跑通，但只在
构造查询上验证过。真实写作里 agent 会怎么提问、`症状` 字段够不够用、
路由表常驻这一层是不是真的省了开销——这些得用起来才知道。

**在这之前继续下沉是凭空猜参数**：万一 `症状` 这个字段设计不成立，
下沉得越多返工越大。

## 待下沉

1. **`01_大纲/README.md` 的七种推导模式 + 展开阶梯**。
   目前最大的一块常驻方法论，下沉后 `_分发/AGENTS.md` 的大纲行可收成一个指针。
2. **`02_人物/人物模板.md` 的主动/被动场景结构**同理。

## 届时要一并评估

- 卡数过 30 后，路由表是否改为按阶段切片常驻（`index --stage` 已支持，
  改的是加载策略不是卡片格式）。
- `症状` 字段在真实提问下的命中率——不理想就要调字段权重或补写方式。
