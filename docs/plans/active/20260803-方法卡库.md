---
model: claude-opus
generated_at: 2026-08-03T00:00:00Z
type: plan
title: 方法卡库——按需加载的方法论体系
---

# 方法卡库

## 要解决的问题

方法论写在常驻文档里，代价随方法论数量线性增长——每一次对话都要带上全部方法，
哪怕这次只用得到其中一条。方法论收得越多，每次推理越贵。

目标是把这条曲线掰平：**知识总量可以一直涨，单次上下文开销基本不涨。**
类比 dense → MoE：路由表常驻，专家按需激活。

## 架构

三层，越往下越贵、越少触发：

| 层 | 是什么 | 常驻？ | 大小 |
|----|--------|-------|------|
| 路由 | `_方法/_索引.md`——每卡一行（一句话 + tags） | 是 | 9 卡约 0.6k 字 |
| 检索 | `method.py find "<自然语言问题>"` → top-k 卡名 + 一句话 | 否 | 单次约 0.2k |
| 展开 | `method.py show <id>` → 整卡 | 否 | 单卡约 1k |

检索是 **BM25 + 中文双字切分**，纯词面、零依赖、无向量。
双链（`相关:`）提供图扩展：`find --hop 1` 带出命中卡的邻居，补词面检索够不到的关联。

## 关键设计决策

### 症状字段

第一版检索有一次明显误排：查询「写到第三卷不知道该写什么了」，
`地形生成法` 压过了 `长篇失速诊断`——两张卡都只命中 `不知`、`什么` 这类功能词双字，
短卡因 BM25 长度归一化胜出。

根因不是参数，是**词汇鸿沟**：作者写卡用术语（"线头过载"），求助者说的是"不知道该写什么了"。
所以每张卡必须写 `症状:`——求助者的原话，权重与标题并列最高。
补上之后同一查询的首位得分 21.5 vs 4.4，五个测试查询全部首位命中。

`method.py check` 把缺 `症状` 当失败拦下：**没有原话钩子的卡，只有作者自己搜得到。**

### 只收方法，不收素材

方法是"怎么做"，素材是"有什么"。生物图鉴、骑士盘点、占卜方式一类不进库——
模型本身就有大量此类常识，收进来既重复又永远收不完，还会把路由表撑坏。

### 常驻的判据不进库

「故事驱动法则」这种任何时候都成立的短判据留在 `docs/writing-style.md`。
**路由的价值在于筛掉当前用不上的东西——永远用得上的东西没有筛的必要。**

### 去重

吸纳前 `method.py dupe "<摘要>"`，按落点判定：同一套步骤换外壳 → 并卡并扩关键词；
多出新判断步骤 → 并卡加步；落点与判据都不同 → 开新卡并互相双链。
`method.py check` 另有全库反查，两张卡过于相似会被拦。

## 已完成

- `scripts/method.py`：find / show / index / dupe / check
- `_方法/` 九张卡：世界观 6、人物 2、复盘 1
- 从 `00_世界观/_设计方法.md` 下沉 5 张；它保留顺序、理念、引导协议，改为导览
- 新增 4 张（政权构建法 / 组织建制法 / 反派设计法 / 长篇失速诊断）
- 注册进 `check_all.py`（第十一项「方法」）
- `_分发/AGENTS.md` 导航加一行，全文 411 → 362 词（同时压实了导航表分隔符）
- 吸纳管线：`ingest` / `skip`，`_未收.md` 台账，卡的 `来源:` 与 `## 沿革`

## 素材吸纳管线

库只解决"用得起"，不解决"长得大"。要能不断自我完善，还得有代谢：

```bash
python scripts/method.py ingest <素材路径>   # 生成加工工单
python scripts/method.py skip "<来源>" --reason ... --revised ...
```

`ingest` 为每份素材配上库里最相关的三张卡（含现有判据）并强制三问。
脚本不蒸馏、不判收不收——只把对比材料摆到位并把问题问出来，判断留给 agent。

### 反向影响是这条管线的重点

第一版只有单向查重：问"库里有没有"，判定重复就丢弃。这会丢掉最有价值的一类反馈——
**素材与已有卡重复，往往正说明那张卡漏了一步或判据站不住。**

实测：`真正的骑士应该是怎样的` 是人物盘点 + 大段广告，无可照做步骤，不该开卡。
但工单第二问逼出了一条反例——乌瑟尔的信条毁掉的是**最忠实的执行者**
（他教出的王子恰因学得太像而堕落），而 `组织建制法` 第 1 步只写了"宗旨会长出反对者"。
于是：不开卡，但改卡 + 记沿革 + `skip --revised 组织建制法`。

所以台账把「不开卡的理由」和「修正过的卡」分成两列。
**不收 ≠ 无影响**，混作一栏就等于把这类反馈丢了。

### 留痕的分工

| 处置 | 痕迹在哪 |
|------|---------|
| 开新卡 | 卡的 `来源:` |
| 改已有卡 | 卡末尾 `## 沿革` + 台账 `--revised` |
| 不开卡 | 台账，`ingest` 下次自动跳过 |

`沿革` 不参与检索——它记的是卡怎么来的，不是它能解决什么。

## 下一批

1. **`01_大纲/README.md` 的七种推导模式 + 展开阶梯下沉为卡**。
   这是目前最大的一块常驻方法论，下沉后 AGENTS.md 的大纲行可以收成一个指针。
2. **`02_人物/人物模板.md` 的主动/被动场景结构**同理。
3. 卡数过 30 后评估路由表是否需要按阶段切片常驻（`index --stage` 已支持，届时改的是加载策略不是格式）。

## 验收

```bash
python scripts/method.py check      # 字段完整、双链有效、索引新鲜、无近重复
python scripts/check_all.py --quiet
```
