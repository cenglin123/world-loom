# Round 1 · converge 评议（引导系统模式感知）

- date: 2026-07-29
- reviewers: 3（general-purpose，fresh context，identical rubric 各侧重不同维度）
  - R1: 概念正确性（逻辑自洽、完整性、概念风险）
  - R2: 工程可行性（兼容性、变更半径、可执行性）
  - R3: 方法论忠实性（以筠系列引用忠实性、适配质量）

## Verdict 汇总

| Reviewer | verdict |
|----------|---------|
| R1 | 需修复 |
| R2 | 需修复 |
| R3 | 需修复 |

并行裁决 3/3 = 需修复。无 conceptual/architectural 级阻断需升级完整收敛。

## 合并收敛阻断集

### Hard Blocks（修订时须全部解决）

| ID | 共识度 | 问题 | 修复方向 |
|----|--------|------|---------|
| B1 | 2/3（R1+H2, R3 不同角度）| **就绪自检硬门控与模式感知的顺序冲突**：AGENTS.md 就绪自检在引导对话之前执行，无限模式用户触发"推进剧情"时被不透明阻断墙挡住。不在本计划 scope 内但必须给出处理方案 | 二选一：(a) 就绪自检加模式感知前置——探测用户模式后决定是否执行完整就绪自检；(b) 接受现状，在引导策略中明确"无限模式用户不建议触发'推进剧情'流程，应走世界建造流程" |
| B2 | 3/3（R1+H2 + R3+H1） | **法则 7 类别混淆**：有限/无限框架是"元框架"（关于设计过程），六条法则是"创作技巧"（关于故事设计）。将 Agent 对话引导规则放入故事驱动法则表属于 category error | 移出故事驱动法则表，在 writing-style.md 另开独立章节「有限/无限框架的引导策略」，与法则表同级但分离 |
| B3 | 1/3（R2+H3，独报） | **AGENTS.md 修改后三文件同步断裂**：修改 AGENTS.md 后须跑 `agent_links.py repair`，否则 CLAUDE.md/GEMINI.md 不同步，pre-commit hook 阻断 | 计划 §4 补充后置操作 |
| B4 | 1/3（R2+H1，独报） | **模板漂移**：主线.md 若新增 mode 字段，`_模板/01_大纲/主线.md` 需同步更新，否则 `template.py reset` 抹除字段 | 移除 mode 字段的持久化方案后，本条自动消解。见 B7。 |
| B5 | 1/3（R2+H2，独报） | **"六条法则"陈旧数字引用**：`_设计方法.md` 和 `writing-style.md` 均写死"六条"，若法则表扩展则需同步更新 | 如按 B2 方案分离法则 7，则法则表仍为六条，本条自动消解 |

### Soft Blocks（修订时优先处理）

| ID | 共识度 | 问题 | 修复方向 |
|----|--------|------|---------|
| B6 | 3/3（R1+H2/R2/S1/R3+S3） | **七点清单的模式依赖未标注**：第 3（叙事空间）、5（故事自发性）、6（可解释性）预设"有故事"，纯无限模式下不适用 | 标注每点的适用模式：P1/P2/P4/P7 全模式适用；P3/P5/P6 有限/混合模式下启用。伏笔密度约束纯无限模式下不适用 |
| B7 | 3/3（R3+F2 + R1+S1 + R2+S2） | **mode 字段持久化矛盾**：一边写入文件（跨对话持久化），一边声称"不是身份标签"。而且生命周期未定义、只写不读 | **移除 mode 字段持久化方案**——不从 §4 落地文件清单中删除 01_大纲/主线.md 的改动行。模式在对话上下文中维护，不写入源码层文件。跨对话模式的恢复依赖 Agent 首次引导时的重新探测 |
| B8 | 2/3（R3+S1 + R1+S3） | **"混合模式"命名与定位问题**：原 Carse 框架只有二元，"混合模式"实际是 fallback 策略。应标注为自主扩展，改名"未确定模式（探测 fallback）"，并补充退出条件 | 改名 + 标注来源 + 补充"当用户后续对话中出现显式倾向信号时退出" |
| B9 | 2/3（R2+S1 + R1/框架风险） | **纯软约束架构风险**：35 行自然语言描述无脚本兜底、无 hook 校验。依赖 Agent 记忆 → 跨对话丢失 | 作为已知技术债务记录在 §5，标注"若观察到此规则在跨对话中失效 ≥3 次，考虑将模式探测固化为 prompt template 文件或脚本的 --mode 参数" |

### 接受（不修）的 soft blocks

| ID | Reviewer | 问题 | 不修理由 |
|----|----------|------|---------|
| — | R1+S2 | 有限模式切换信号阈值未论证（"停滞≥2轮"） | 阈值为启发式起点，非科学结论。先上线观察，后续根据实际使用数据调整 |
| — | R1+S4 | 探测问题措辞不对称 | 同意存在轻度偏向，但"写一小段场景"的低风险入口设计是故意的——降低有限模式的进入门槛正是本计划的目标之一，不对称服务于可用性 |
| — | R1+S5 | "新主题"边界未定义 | 约定由 Agent 按常识判断——"新建世界观/首次做大纲/首次建角色"三个明确入口。细分场景（新建种族 vs 新建城市）不触发重新探测，沿用当前标注 |
| — | R3+S4 | "轻量切换" vs 原框架"存在论级别"切换 | 这是有意的适配决策，在 §1.3 声明为适配而非原框架直接映射。不做更多修改 |
| — | R2+S3 | 验收项 5 不可验证 | 降为"建议验证"，不作为硬验收标准。正式验收以 R2-S1 的方案为基础——后续若实现 prompt template，可机械验证 |

### Flags（不阻塞，供后续参考）

- F1（R1+F1）：七点清单引用的来源一致性校验——接受，来源已在正文标注"评论区"。
- F2（R1+F2）：同对话多模式并存——作为技术债务记录，现阶段会话级探测够用。
- F3（R1+F3）：有限模式侧的镜像风险（用户从不扩展世界观）——补充到 §3.5。
- F4（R2+F1）：治理文档变更需 [governance] 标记——补充到 §4。
- F5（R2+F2）：外部引用推理链未在计划内闭环——接受，retrospective 时补。
- F6（R3+F3）：避坑与策略之间的判据缺失——在 §3.5 补充"默认阈值为连续 3 轮纯推演世界而无任何故事/大纲/角色产出"。
- F7（R3+F5）：七点清单与核心主题的弱关联——接受，标注为"独立增益"而非模式感知的组成部分。

## 修订决策

1. **法则 7 → 独立章节**（B2）
2. **移除 mode 字段持久化**（B7）
3. **七点清单标注模式适用性**（B6）
4. **"混合模式" → "未确定模式（探测 fallback）"**，标注为自主扩展，补充退出条件（B8）
5. **就绪自检冲突**：二选一 chose (b)——在引导策略中明确"无限模式用户不宜触发'推进剧情'流程，应走世界建造流程"（B1-b）。理由：(a) 方案改动 AGENTS.md 就绪自检，变更半径大且触及项目治理核心，超出本计划 scope。
6. **软约束架构风险**（B9）：记录为技术债务，不阻塞当前执行

---

## Round 2 · 验收审查

- reviewer: R4（general-purpose，fresh context）
- 审查范围：B1-B9 逐条验证
- verdict: **可收敛**

### 逐条验收

| ID | 状态 | 证据摘要 |
|----|------|---------|
| B1 | resolved | §1.2+§3.2+§4 三处一致：无限用户不触发推进剧情管道 |
| B2 | resolved | §4 明确独立章节不并入法则表 |
| B3 | resolved | §4 补充 agent_links.py repair 后置操作 |
| B4 | resolved | mode 字段持久化已移除，前提条件拆除 |
| B5 | resolved | 法则表维持六条，自动消解 |
| B6 | resolved | §3.4 逐点标注适用模式列 |
| B7 | resolved | §4 移除项声明模式限会话级 |
| B8 | resolved | 改名+标注来源+退出条件，三项均落实 |
| B9 | resolved | §5 新增技术债务记录，含触发条件和升级路径 |

### 新引入问题检查

无。三文档落地位置职责分离清晰，未确定模式退出条件与七点清单标注不自相矛盾，有限模式侧保护与最小设定量原则不冲突。
