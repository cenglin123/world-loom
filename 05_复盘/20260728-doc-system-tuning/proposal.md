# 文档体系微调提案（R1 修订）

> 对象：AGENTS.md + docs/audit-checklist.md
> 类型：非小说 converge（治理文档修改）
> 修订轮次：R1 — 吸收了 reviewer 6 项发现（H1/H2/S1/S2/S3/S4 + F1/F2/F3）

---

## A. AGENTS.md 硬约束段：标注脚本覆盖范围

### 当前（L77-86）

```
### 硬约束

**pre-commit / commit-msg / pre-push 三板斧**——具体检查逻辑见各 hook 脚本自身（.githooks/），此处只记哪些事**没有脚本兜底**：

- **治理文档**：...
- **人设演化记载**：...
- **伏笔登记**：...
- **记忆回写**：...
- **关系同步**：...
- **标签同步**：...
```

### 改为

每条约束末尾标注脚本覆盖的层面——agent 仍需主动执行所有条目，但完工时可以知道哪些层面脚本会帮你查漏：

```
### 硬约束

- **人设演化记载**：剧情导致人物卡「当前稳定内核」或「身份矛盾」任一字段改变 → 必须在「演化记录」追加触发事件、付出代价、旧状态与新状态；禁止覆盖历史。（无脚本兜底——全靠你）
- **记忆回写**：每卷/关键场景后，受影响角色的 `02_人物/<角色名>.md` 记忆段必须更新。（无脚本兜底——全靠你）
- **伏笔登记**：新伏笔 → `04_伏笔/伏笔登记表.md` 登记（状态=open），回收后 → closed。不登记 = 不存在 = 收不回来。（脚本兜格式：`check_all.py` 检查已登记伏笔的列数/状态值/超期；但**新伏笔是否登记**是语义判断，脚本查不出——由你负责）
- **关系同步**：角色间有意义互动后，`python scripts/relationship.py set/update` 写入关系 JSON。卷末 `regenerate` 重建矩阵。（脚本兜格式：`check_all.py` 检查 JSON 字段完整性；但**关系是否需要更新**由你判断）
- **标签同步**：角色创建或能力变化后更新 frontmatter tags。卷末 `python scripts/check_tags.py regenerate`。（脚本兜完整性：`check_all.py` 检查四类标签是否齐全）
- **治理文档**：修改 `.githooks/commit-msg` 的 `GOVERNANCE_FILES` 清单内任一文件 → commit message 必须含 `[governance]` 标记。清单为该文件的唯一权威源。（`commit-msg` hook 拒绝缺失标记的提交——在 `git commit` 时触发，不在 `check_all.py` 中）
```

### 改动说明

- 不再分"脚本兜底 vs 不兜底"两节——避免 S2（行为规则下出现"不需要记住"的逻辑矛盾）
- 不再声称脚本能替代 agent 的语义判断——每条明确标注脚本覆蓋的层面（格式 vs 语义）
- 保持原"硬约束"标题和全部 6 条内容不删减——无信息丢失
- 价值主張从"减负"调整为"明确分工"——agent 仍需记住全部 6 条，但知道完工检查能捕获什么

---

## B. 四阶段工作流 §②：区分「必须」和「可用工具」

### 当前（L110-118）

```
### ② 写前准备

为每场戏整理上下文包（~300-500 字），写入 `03_正文/第N卷/_准备_第M章.md`：

1. 确定在场角色列表
2. `python scripts/recall.py <角色1> <角色2>` — 获取人物生成内核 + L2 写前注入集（agent 压缩转写为自然语言，不机械粘贴输出）
3. `python scripts/relationship.py show <角色名>` — 两两关系摘要
4. 将以上与本场大纲、场景初始状态、**场景决策**...和**达成断言**组装为自然语言；达成断言至少 1 条...
```

### 改为

```
### ② 写前准备

为每场戏整理上下文包（~300-500 字），写入 `03_正文/第N卷/_准备_第M章.md`。

**必须做**：
1. 确定在场角色列表
2. 注入角色当前状态——查阅 `02_人物/<角色名>.md` 的记忆段 + `02_人物/relationships.json` 的关系摘要
3. 将以上与本场大纲、场景初始状态、**场景决策**（Belief / Desire / Intention / Obstacle / Tactic / 换挡条件 / 可接受代价）和**达成断言**组装为自然语言；达成断言至少 1 条，并覆盖在场角色的本场目标及一个相关的人设字段

**快捷工具**（非必须——替代手动查阅，效果相同）：
- `python scripts/recall.py <角色1> <角色2>` — 获取多角色的 L2 写前注入集摘要。**输出需 agent 压缩转写为自然语言，不机械粘贴。**
- `python scripts/relationship.py show <角色名>` — 两两关系摘要
```

### 改动说明

- 区分"必须做"（目标）和"快捷工具"（手段）——agent 可以手动翻文件达到同样效果，不被工具调用束缚
- 保留 recall.py 的多角色参数（`<角色1> <角色2>`——修复 F2/S3）和输出处理指导（修复 S3）
- 步骤从编号列表改为分层——让 agent 理解核心目的是"注入角色状态"，工具是路径之一

---

## C. audit-checklist 小说专项审计：标注主导阻断级别

### 当前

10 个维度平铺在同一节下，无显式标注哪些以硬阻断为主、哪些以软判断为主。

### 改为

每个维度加一行级别标注，不改变 checkboxes 内容：

```
## 小说专项审计（agent 手动裁决）

### 世界观一致性（以硬阻断为主——违反物理层即阻断）
- [ ] ...
### 人设一致性（混合——事实层硬阻断，动机解释层软判断）
- [ ] ...
### 记忆一致性（以硬阻断为主——矛盾条目客观可判定）
- [ ] ...
### 关系一致性（混合——格式硬阻断，更新必要性软判断）
- [ ] ...
### 伏笔一致性（混合——超期硬阻断，回收合理性软判断）
- [ ] ...
### 时间线一致性（以硬阻断为主——倒流/同时两地客观可判定）
- [ ] ...
### 开篇结构（可读性技法阈值——以软判断为主，部分字数/位置可机械验证）
- [ ] ...
### 结构与工具（观察项——不阻断）
- [ ] ...
```

### 改动说明

- 不在维度之上加分组标题（修复 S1——不对整个维度做"硬阻断"断言）
- 改为在每个维度标题后加一行括号标注，标注该维度的主导阻断级别
- 承认混合性质（"混合——事实层硬阻断，动机解释层软判断"——回应 S1）
- 措辞使用"以...为主"而非"无灰色地带"——回应 F3
- 零改动 checkboxes 内容

---

## 影响范围

| 文件 | 改动量 | 类型 |
|------|--------|------|
| `AGENTS.md` | ~25 行改写（硬约束段 + §②） | 治理文档 → commit 需 `[governance]` |
| `docs/audit-checklist.md` | 加 8 行级别标注，0 行内容变更 | 非治理文档 |

## R1 修订记录

- H1：移除「治理文档标记由 check_all.py 检查」的错误断言，更正为 commit-msg hook
- H2：不再声称脚本兜底伏笔登记/关系同步的语义层，改为标注「脚本兜格式，语义由你负责」
- S1+S3：audit-checklist 维度标注改为括号式主导级别标注，不再做全维度断言
- S2+S4：放弃"脚本兜底 vs 不兜底"二分结构，改为逐条标注——价值主張从"减负"调整为"明确分工"
- S3+F2：保留 recall.py 多角色参数和输出处理指导
- F1：保留"硬约柬"原标题
