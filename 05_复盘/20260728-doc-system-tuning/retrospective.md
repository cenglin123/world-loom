# 收敛回溯

> 触发：文档体系分析与微调（指南层 vs 机制层分离优化）
> 日期：2026-07-28
> 轮数：2 轮（R1 需修复 → R2 可收敛）

## 改动总结

1. **AGENTS.md 硬约束段**：6 条约束逐条标注脚本覆盖范围（格式层 vs 语义层），明确 agent 和脚本的分工。保留全部 6 条，信息零丢失。
2. **AGENTS.md §② 写前准备**：区分「必须做」（角色状态注入）和「快捷工具」（recall.py / relationship.py），让 agent 自由选择路径。
3. **docs/audit-checklist.md**：8 个小说专项审计维度加括号标注主导阻断级别（硬阻断为主 / 混合 / 软判断为主 / 观察项）。

## 收敛路径

- R1：2 硬阻断（H1 check_all.py 行为错误断言、H2 脚本语义覆盖范围过度声称）+ 4 软阻断 → 全面修订
- R2：0 硬阻断、0 软阻断、3 轻量 flag → 可收敛。flag 在执行时纳入（F1 措辞对齐、F2 保留人物生成内核）

## 最终状态

- AGENTS.md：~155 行（改动 ~20 行）
- docs/audit-checklist.md：8 个维度标注，0 内容变更
- check_all.py --quiet 同步项通过
